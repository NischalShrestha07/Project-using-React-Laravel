import React from 'react'
import Navbar from './Navbar/Navbar'

const Button = (props) => {
    // const { name } = props
    return (
        <>
            <Navbar />

            <button > Hello</button>
        </>
    )
}

// Button.prototype = {
//     name: PropTypes.string
// }
export default Button
